---
import Base from "../../layouts/Base.astro";
import SectionIndex from "../../components/SectionIndex.astro";
---
<Base title="Instances" description="The two Foundries that realize the pattern — and the diff that earns its abstraction.">
  <SectionIndex
    section="instances"
    kicker="Instances"
    title="Instances"
    intro="The pattern, twice. Two Foundries share a spine and differ on one axis — the gate. The Diff lays the two side by side and shows what generalized."
  />
</Base>
