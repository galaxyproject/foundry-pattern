---
import Base from "../../layouts/Base.astro";
import SectionIndex from "../../components/SectionIndex.astro";
---
<Base title="The Pattern" description="What a Foundry is — the definition behind every instance.">
  <SectionIndex
    section="pattern"
    kicker="The Pattern"
    title="The Pattern"
    intro="The descriptive spec: a knowledge base of Molds, cast into frozen artifacts with provenance — a pattern each domain instantiates and extends. Start with the Story for why a Foundry exists, the Model for how it works, and the Guiding Principles behind it — then Principles in Action for those principles incarnated in the two instances' real decisions, and Anatomy for what every instance shares versus what each domain adds. Setting up a Foundry turns Anatomy into steps. The Glossary is the reference."
  />
</Base>
