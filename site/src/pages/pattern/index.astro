---
import Base from "../../layouts/Base.astro";
import SectionIndex from "../../components/SectionIndex.astro";
---
<Base title="The Pattern" description="What a Foundry is — the definition behind every instance.">
  <SectionIndex
    section="pattern"
    kicker="The Pattern"
    title="The Pattern"
    intro="Start with the Story for why a Foundry exists, the Model for how it works, then Anatomy for what every instance shares versus what each domain adds. Guiding Principles and the Glossary are the reference."
  />
</Base>
